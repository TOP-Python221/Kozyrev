  ====================  легенда  ====================

Верно      — всё хорошо и комментариев не требует
Замечания  — в целом верно, но можно лучше / комментарии
Ошибки     — что-то или всё пошло не так
Неверно    — условие задачи неверно интерпретировано
Нет ответа — файл с решением задачи отсутствует



  =====================  задачи  ====================

1.1. Замечания: 
        а) отсутствует вывод выполненного скрипта

1.2. Замечания: 
        а) отсутствует вывод выполненного скрипта

1.3. Замечания: 
        а) а здесь точно нужны преобразования int() и ord()?
        б) отсутствует вывод выполненного скрипта

1.4. Замечания: 
        а) скобки лишние: приоритет оператора and меньше, чем приоритеты операторов сравнения
        б) отсутствует вывод выполненного скрипта

2.1. Замечания: 
        а) отсутствует вывод выполненного скрипта

2.2. Замечания: 
        а) зачем диапазон от 1 до number+1? чем хуже range(number)?
        б) отсутствует вывод выполненного скрипта

2.3. Ошибки:
        а) в Python нет оператора инкремента ++а
        здесь то же самое делается с помощью оператора расширенного присваивания а += 1
        б) в Python цикле for у нас уже есть диапазон чисел, именно из него числа присваиваются в переменную цикла на каждой итерации
     Замечания: 
        а) не слишком оптимально перебирать все числа до number включительно, т.к. делитель никогда не может быть больше number // 2
        б) отсутствует вывод выполненного скрипта

2.4. Замечания: 
        а) когда переменная цикла и её значение нам не нужны в явном виде, то мы можем её "спрятать":
        
            for _ in range(n):
        
        б) отсутствует вывод выполненного скрипта

2.5. Замечания: 
        а) отсутствует вывод выполненного скрипта

2.6. Замечания: 
        а) отсутствует вывод выполненного скрипта

2.7. Замечания: 
        а) не смешивайте переменную цикла и переменную для ввода – это должны быть разные переменные
        б) проанализируйте, какие переменные сколько раз используются, и в каких действительно есть нужда в контексте данной задачи
        
            res = 0
            for _ in range(int(input('numbers: '))):
                res += int(input())
            print(res)
        
        в) отсутствует вывод выполненного скрипта

2.8. Нет ответа



В целом, если сами писали код, то неплохо.

Обязательно запускайте код и проверяйте на разных входных данных!
Думайте о разных вариантах входных данных, на чём ваша программа может "споткнуться".

Пересмотреть запись лекции по циклам, перечитать материал о функции range()

Изучить таблицу приоритета операторов:
https://docs.python.org/3/reference/expressions.html#operator-precedence



  =====================  баллы  =====================

1.1. 2/2
1.2. 2/2
1.3. 2/3
1.4. 2/2
2.1. 2/2
2.2. 2/2
2.3. 2/3
2.4. 3/3
2.5. 3/3
2.6. 3/3
2.7. 3/4
2.8. 0/3